apiVersion: v1
kind: Service
metadata:
  name: openvpn
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: openvpn
  type: NodePort
  ports:
    - port: 1194
      protocol: UDP
      targetPort: 1194
      nodePort: {{ .Values.nodePort }}
