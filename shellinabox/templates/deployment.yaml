apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.name }}
    spec:
      containers:
        - name: shellinabox
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: SIAB_PASSWORD
              value: {{ .Values.env.SIAB_PASSWORD }}
            - name: SIAB_SUDO
              value: {{ .Values.env.SIAB_SUDO }}
            - name: SIAB_GROUPID
              value: 1010
            - name: SIAB_USERID
              value: 1010
            - name: SIAB_GROUP
              value: shellinabox
            - name: SIAB_USER
              value: shellinabox
          ports:
            - containerPort: 4200
              name: http
